VITE_SUPABASE_URL=
# Dashboard → Project Settings → API Keys → Publishable
VITE_SUPABASE_PUBLISHABLE_KEY=

# Solo para `npm run pick` y el cron. Nunca VITE_ y nunca en el frontend.
# Dashboard → Project Settings → API Keys → Secret
SUPABASE_SECRET_KEY=
